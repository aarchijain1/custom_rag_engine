# Core LLM & Orchestration
langchain
langchain-google-genai
langgraph

# MCP Protocol - HTTP Transport
mcp
starlette
uvicorn
sse-starlette
httpx

# Vector Store & Embeddings
chromadb
sentence-transformers

# Document Processing
pypdf
python-docx

# Utilities
python-dotenv
# Core dependencies for streaming MCP server
fastapi>=0.104.0
uvicorn>=0.24.0
sse-starlette>=1.8.2
httpx>=0.25.0

# Existing dependencies (if not already installed)
# Add your other requirements here:
# langchain
# chromadb
# sentence-transformers
# etc.